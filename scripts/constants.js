const WIDTH = 800;
const HEIGHT = 600;

// enums
const TEXTURES = {
    players: {
        warrior: {
            walk: "warrior_walk",
            idle: "warrior_idle",
            attack: "warrior_attack",
        }
    }
}