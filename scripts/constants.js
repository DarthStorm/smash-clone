const WIDTH = 1400;
const HEIGHT = 600;

// enums
const TEXTURES = {
    players: {
        warrior: {
            walk: "warrior_walk",
            idle: "warrior_idle",
            attack: "warrior_attack",
        }
    },
    bullets: {
        bullet: "bullet"
    }
}

const ANIMS = {
    
}